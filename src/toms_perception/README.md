# toms_perception

Perception adapter: object detection, segmentation, pose estimation.

## Contents
- `adapter.py` – `PerceptionAdapterBase` (abstract) + `MockPerceptionAdapter`

## TODO (hardware)
- Implement a concrete adapter using YOLO/FoundationPose/etc.
- Update `perception.yaml` with the chosen backend and model paths.
