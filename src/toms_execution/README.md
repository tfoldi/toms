# toms_execution

Action execution sequences: approach, grasp, place, retreat.

## Contents
- `executor.py` – `ManipulationExecutor` (composes robot bridge calls)

## TODO (hardware)
- Implement Cartesian waypoint computation (commented out in executor.py).
- Wire in real robot bridge once `robot.yaml` is complete.
